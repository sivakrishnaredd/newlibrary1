def sum(a,b)
{
  echo "the sum of ${a} and ${b} is ${a+b}"
}
def sub(a,b)
{
  echo "the subtraction result of ${a} and ${b} is ${a-b}"
}
def mul(a,b)
{
  echo "the priduct of ${a} and ${b} is ${a*b}"
}
def div(a,b)
  {
    echo "the division of ${a} and ${b} is ${a/b}"
  }
